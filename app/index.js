import handleEvents from './app.js';
import { printHistories } from './history/index.js';
import {
  getPrompt, removePrompt, setPrompt,
} from './prompt/index.js';

export {
  handleEvents,
  printHistories,
  getPrompt,
  removePrompt,
  setPrompt,
};
